# Borrow Buddy: Tasks

อ้างอิง [CONTEXT.md](./CONTEXT.md) และ [design.md](./design.md) ทำตามลำดับ ติ๊กเมื่อเสร็จ

กติกาการทำงาน: JavaScript เท่านั้น, ห้ามลบไฟล์โดยไม่ถามก่อน, ตอนตั้งค่า Vite/React/Vitest ให้ตรวจเอกสารล่าสุดผ่าน Context7 ก่อน

## เฟส 1: ตั้งโปรเจ็กต์

- [x] T1.1 สร้างโปรเจ็กต์ Vite + React (เทมเพลต JavaScript) ในโฟลเดอร์ `borrow-buddy` โดยไม่ทับ `CONTEXT.md` และ `design.md`
- [x] T1.2 ติดตั้ง Vitest และเพิ่มสคริปต์ `test` ใน `package.json`
- [x] T1.3 สร้างโครงโฟลเดอร์ `src/components` และ `src/lib` ตาม design.md ข้อ 7

- **เสร็จเมื่อ:** `npm run dev` เปิดหน้าเปล่าได้ และ `npm test` รันได้

## เฟส 2: ตรรกะ (`src/lib`) ทำแบบเขียนเทสต์ก่อน

- [x] T2.1 `dateFormat.js`: แปลง `YYYY-MM-DD` เป็น พ.ศ. แบบ "24 ก.ย. 2569" + เทสต์
- [x] T2.2 `loanRules.js`: คำนวณสถานะ (ยังไม่คืน / เกินกำหนด / คืนแล้ว) รับ "วันนี้" เป็นพารามิเตอร์ + เทสต์ขอบ (วันนี้ = กำหนดคืนต้องยังไม่เกิน, วันถัดไปเกิน)
- [x] T2.3 `loanRules.js`: จำนวนวันเกินกำหนด + เทสต์
- [x] T2.4 `loanRules.js`: ตรวจความถูกต้องของ Loan (ชื่อเพื่อน/ของห้ามว่าง, กำหนดคืนและวันที่คืนจริงต้องไม่ก่อนวันที่ยืม) คืนข้อความผิดพลาดภาษาไทย + เทสต์ทุกกรณีผิด
- [x] T2.5 `loanRules.js`: จัดกลุ่ม เกินกำหนด → ยังไม่คืน → คืนแล้ว และเรียงตามกำหนดคืนใกล้สุดในแต่ละกลุ่ม + เทสต์
- [x] T2.6 `loanRules.js`: กรองตามชื่อเพื่อน + เทสต์
- [x] T2.7 `loanRules.js`: กดคืนแล้ว (ค่าเริ่มต้นวันนี้ แก้ย้อนหลังได้) และยกเลิกการคืน + เทสต์
- [x] T2.8 `storage.js`: อ่าน/เขียน Loan ใน localStorage คีย์เดียว, JSON เสียหรืออ่านไม่ได้ให้คืนรายการว่างพร้อมสัญญาณแจ้งเตือน ไม่เขียนทับข้อมูลเดิม

- **เสร็จเมื่อ:** `npm test` ผ่านทั้งหมด

## เฟส 3: UI

- [x] T3.1 `App.jsx`: เก็บ state Loan โหลดจาก storage ตอนเปิด บันทึกทุกครั้งที่เปลี่ยน และแสดงข้อความภาษาไทยเมื่ออ่านข้อมูลไม่ได้
- [x] T3.2 `LoanForm.jsx`: ฟอร์มเดียวใช้เพิ่มและแก้ไข (เพื่อน, ของ, วันที่ยืม ค่าเริ่มต้นวันนี้, กำหนดคืน) แสดงข้อผิดพลาดจากการตรวจ มีปุ่มยกเลิกการแก้ไข
- [x] T3.3 `SearchBox.jsx`: ช่องค้นหาตามชื่อเพื่อน
- [x] T3.4 `LoanList.jsx` และ `LoanItem.jsx`: แสดงกลุ่มและรายการตาม design.md ข้อ 6 (วันที่ พ.ศ., จำนวนวันเกินเฉพาะเกินกำหนด, วันที่คืนจริงเฉพาะคืนแล้ว)
- [x] T3.5 ปุ่มต่อรายการ: คืนแล้ว (เลือกวันที่ได้), ยกเลิกการคืน, แก้ไข ไม่มีปุ่มลบ
- [x] T3.6 `ThemeToggle.jsx`: โหมดมืด สลับได้และจำค่าไว้
- [x] T3.7 สไตล์: รองรับมือถือ, สถานะแยกด้วยข้อความและสี (ไม่พึ่งสีอย่างเดียว)

- **เสร็จเมื่อ:** ใช้งานครบทุกอย่างในขอบเขตผ่านหน้าเว็บได้

## เฟส 4: ตรวจรับ

- [x] T4.1 รัน `npm test` ผ่านทั้งหมด
- [x] T4.2 ทดลองในเบราว์เซอร์: เพิ่ม → แก้ไข → กดคืน → ยกเลิกคืน → รีเฟรชแล้วข้อมูลยังอยู่
- [x] T4.3 ทดลองกรณีขอบ: กำหนดคืนวันนี้, กำหนดคืนเมื่อวาน, กรอกวันที่ผิดกติกา, ข้อมูลใน localStorage เสีย
- [x] T4.4 ตรวจหน้าจอมือถือและโหมดมืด
- [x] T4.5 ตรวจว่าไม่มีฟีเจอร์นอกขอบเขต (ลบ, แจ้งเตือน, เลื่อนกำหนด, รูปภาพ, สำรองข้อมูล) และศัพท์ในหน้าเว็บตรงกับ CONTEXT.md

# เวอร์ชัน 2: Supabase + เข้าสู่ระบบ

อ้างอิง [design.md](./design.md) (เวอร์ชัน 2) และ [CONTEXT.md](./CONTEXT.md) ตอนทำงานที่เกี่ยวกับ Supabase ให้ตรวจเอกสารล่าสุดผ่าน Context7 ก่อน ห้ามใช้ service role key ในฝั่งเว็บ ห้ามใส่ความลับใน git และห้ามมีการลบ Loan

## เฟส 5: ตั้งค่า Supabase (ฐานข้อมูลและความปลอดภัย)

- [x] T5.1 ยืนยันโปรเจ็กต์ Supabase ที่จะใช้ และตรวจตารางเดิมก่อนแก้อะไร
- [x] T5.2 สร้างตาราง `loans` (migration): คอลัมน์ตาม design.md ข้อ 4/8, `owner_id default auth.uid()`, ข้อจำกัดวันที่และชื่อไม่ว่าง
- [x] T5.3 เปิด RLS และสร้าง policy `SELECT/INSERT/UPDATE` เฉพาะ `authenticated` และ `owner_id = auth.uid()` **ไม่สร้าง policy DELETE** ไม่ให้สิทธิ์ `anon`
- [x] T5.4 สร้างบัญชีเจ้าของด้วย email + password จาก Dashboard (Add user, ไม่บันทึกรหัสผ่านในไฟล์ใด ๆ) (สร้างแล้ว ตรวจพบ 1 บัญชี ยืนยันอีเมลแล้ว) | **ตัดสินใจ 2026-09-25: ไม่ปิดการสมัครเอง (ยอมรับความเสี่ยง) `disable_signup` ยังเป็น false** เว็บไม่มีหน้าสมัคร
- [x] T5.5 รัน advisors ของ Supabase (security/performance) และแก้คำเตือนที่เกี่ยวข้อง
- [x] T5.6 ทดสอบ RLS: ไม่ล็อกอินอ่าน/เขียนไม่ได้, บัญชีอื่นไม่เห็น/แก้ Loan ของเรา, ลบไม่ได้, แก้ `owner_id` ไม่ได้ (ใช้บัญชีทดสอบชั่วคราวที่สร้างจาก Dashboard แล้วเก็บกวาดโดยถามก่อนลบ)

- **เสร็จเมื่อ:** ตาราง `loans` พร้อม RLS ผ่านการทดสอบ T5.6 ทุกข้อ และมีบัญชีเจ้าของ (T5.4)

## เฟส 6: ตรรกะฝั่งเว็บ (`src/lib`) เขียนเทสต์ก่อนในส่วนที่เป็นตรรกะล้วน

- [x] T6.1 ติดตั้ง `@supabase/supabase-js`, สร้าง `.env.example` (`VITE_SUPABASE_URL`, `VITE_SUPABASE_PUBLISHABLE_KEY`), ตรวจว่า `.env` อยู่ใน `.gitignore`
- [x] T6.2 `supabaseClient.js`: สร้าง client จาก env และแจ้งข้อผิดพลาดชัดเจนเมื่อ env ขาด
- [x] T6.3 `loanRepo.js`: ฟังก์ชันแปลง snake_case <-> camelCase + เทสต์
- [x] T6.4 `loanRepo.js`: ดึงรายการ, เพิ่ม, แก้ไข (รวมกดคืน/ยกเลิกคืน) ไม่มีฟังก์ชันลบ, คืนข้อผิดพลาดเป็นข้อความภาษาไทย
- [x] T6.5 `auth.js`: `signIn(email, password)`, `signOut()`, ดู/ติดตามสถานะ session (ไม่มีฟังก์ชันสมัคร)
- [x] T6.6 `legacyImport.js`: อ่าน Loan เดิมจาก localStorage, ตรวจด้วย `loanRules.js`, รายงานรายการที่ข้าม, JSON เสียไม่ทำให้พัง + เทสต์ (ไม่ลบข้อมูลเดิม)
- [x] T6.7 เลิกใช้ `storage.js` เดิมในแอป (ไม่ลบไฟล์ ถามก่อนถ้าจะลบ)

- **เสร็จเมื่อ:** `npm test` ผ่านทั้งหมด (รวมเทสต์ใหม่) และ `lib/` ใหม่ไม่มีการลบ Loan

## เฟส 7: UI เวอร์ชัน 2

- [x] T7.1 `LoginForm.jsx`: email + password, ข้อความผิดพลาดภาษาไทย, ไม่มีลิงก์สมัคร
- [x] T7.2 `App.jsx`: ตรวจ session ตอนเปิด แสดงหน้าเข้าสู่ระบบหรือหน้า Loan, ปุ่มออกจากระบบ, สถานะกำลังโหลด/บันทึก/ผิดพลาด, session หมดอายุกลับหน้าเข้าสู่ระบบ
- [x] T7.3 เปลี่ยน state Loan ให้โหลด/เขียนผ่าน `loanRepo.js` (เขียนสำเร็จจึงอัปเดตหน้าจอ, ล้มเหลวไม่ทิ้งข้อมูลในฟอร์ม)
- [x] T7.4 ขั้นตอนย้ายข้อมูลเดิม: ถามเจ้าของก่อน, แสดงผลนำเข้า/รายการที่ข้าม, ทำครั้งเดียว
- [x] T7.5 สไตล์หน้าเข้าสู่ระบบ: รองรับมือถือและโหมดมืด

- **เสร็จเมื่อ:** เข้าสู่ระบบแล้วใช้งานครบทุกอย่างในขอบเขตผ่านข้อมูลบน Supabase ได้

## เฟส 8: ตรวจรับ เวอร์ชัน 2

- [x] T8.1 รัน `npm test` ผ่านทั้งหมด
- [ ] T8.2 (ยังไม่ได้ทดลอง: ล็อกอินในแท็บที่ควบคุมไม่ได้) ทดลองในเบราว์เซอร์: เข้าสู่ระบบ → เพิ่ม → แก้ไข → กดคืน → ยกเลิกคืน → รีเฟรชข้อมูลยังอยู่ → ออกจากระบบแล้วเห็นเฉพาะหน้าเข้าสู่ระบบ
- [ ] T8.3 ทดลองกรณีผิดพลาด: รหัสผ่านผิด, session หมดอายุ, ออฟไลน์ตอนบันทึก, ย้ายข้อมูลเดิม (ปกติ / JSON เสีย / มีรายการไม่ผ่านการตรวจ)
- [ ] T8.4 ตรวจว่าเปิดจากเบราว์เซอร์/เครื่องอื่นแล้วเห็นข้อมูลเดียวกัน และบัญชีอื่นไม่เห็นข้อมูลของเรา
- [ ] T8.5 ตรวจหน้าจอมือถือและโหมดมืดของหน้าเข้าสู่ระบบ (ตรวจโหมดมืดในเบราว์เซอร์แล้ว ผ่าน ส่วนมือถือยังไม่ได้ตรวจด้วยความกว้างจริง)
- [x] T8.6 ตรวจว่าไม่มีปุ่มลบ, ไม่มีหน้า/โค้ดสมัครสมาชิกในเว็บ, ไม่มี service role key/รหัสผ่านใน repo และศัพท์ในหน้าเว็บตรงกับ CONTEXT.md

## เฟส 9: ปรับ UI เป็นแท็บ ใช้ง่ายบนมือถือ และเพิ่มสีสัน (เวอร์ชัน 2.1)

อ้างอิง design.md ข้อ 6 (v2.1) ห้ามเพิ่มฟีเจอร์นอกขอบเขต ห้ามมีปุ่มลบ ศัพท์ในหน้าเว็บต้องตรง CONTEXT.md

- [x] T9.1 ปรับ design.md ข้อ 6/7/9 และเพิ่มเฟสนี้ใน Tasks.md
- [x] T9.2 `loanTabs.js` (แท็บเริ่มต้น, แท็บตามสถานะ) + เทสต์ และ `TabBar.jsx`; `LoanList.jsx` แสดงเฉพาะสถานะที่เลือกพร้อมข้อความเมื่อว่าง; `App.jsx` แบ่ง 4 แท็บ (ฟอร์มอยู่ในแท็บเพิ่มการยืม, แก้ไข/บันทึก/ยกเลิกสลับแท็บตาม design)
- [x] T9.3 สไตล์: หัวเรื่องไล่สี, พื้นหลังไล่สี (สว่าง/มืด), การ์ดและแท็บมีสีประจำสถานะ, แถบแท็บติดล่างบนมือถือ, ปุ่ม/ช่องกรอกอย่างน้อย 44px
- [ ] T9.4 ตรวจ: `npm test`, lint, build ผ่าน (ผ่านแล้ว 118 เทสต์) และตรวจหน้าตาที่ความกว้างมือถือและโหมดมืด **ค้าง: ผู้ใช้สั่งข้ามการทดสอบหน้าตา (2026-09-25) ยังไม่ได้ดูในเบราว์เซอร์จริง**

- **เสร็จเมื่อ:** ใช้งานผ่านแท็บได้ครบทุกอย่างเดิม อ่านง่ายบนมือถือทั้งโหมดสว่างและมืด
